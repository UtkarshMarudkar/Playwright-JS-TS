# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api.spec.js >> api learning @api
- Location: tests\api.spec.js:4:1

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 200
Received: 400
```

# Test source

```ts
  1  | 
  2  | import { test, expect } from "playwright/test";
  3  | 
  4  | test("api learning @api", async({request}) =>{
  5  |     let authEndpoint = "http://localhost:4000/api/auth/login";
  6  |     let authResponse = request.post(authEndpoint,{
  7  |         data:{
  8  |         "email": "{{adminEmail}}",
  9  |         "password": "{{adminPassword}}"
  10 |     }
  11 |     });
> 12 |     expect(await (await authResponse).status()).toBe(200);
     |                                                 ^ Error: expect(received).toBe(expected) // Object.is equality
  13 |     console.log(await authResponse.json().data.token);
  14 | });
```